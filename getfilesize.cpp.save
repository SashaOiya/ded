#include <stdio.h>

int GetFileSize ( FILE * f );

/*

fread(...)
GetFileSize
fread(...)    

*/

int GetFileSize ( FILE * f )
{
    // save position
    fseek ( f, 0, SEEK_END );
    size_t sizet = ftell ( f );
    fseek ( f, 0, SEEK_SET );

    // restore position
    return sizet;
}
